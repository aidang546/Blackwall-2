__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Local secrets & machine-specific state
config.local.yaml
.erebus_token
*.log

# Downloaded model weights
models/
*.onnx
*.bin
*.gguf

# Audio scratch
*.wav
!erebus/server/static/sfx/*.wav

# Personal briefing data - your business, your numbers, your training
profile.local.yaml
journal.local.jsonl
health.local.jsonl
memory.local.jsonl
audit.local.jsonl

# Vault key. Machine-bound, but it is still a key.
.erebus_key

# Captured evidence. Case files are the operator's, and can contain anything.
cases/

# Health exports, in case one gets dropped in the repo folder. These are the
# most sensitive files the project touches and none of them belong in git.
export.xml
export_cda.xml
apple_health_export/
*health*.csv
*sleep*.csv
*cycles*.csv
*activities*.csv
*_export.csv
*.fit
*.tcx
